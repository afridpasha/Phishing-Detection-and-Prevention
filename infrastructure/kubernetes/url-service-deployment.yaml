apiVersion: apps/v1
kind: Deployment
metadata:
  name: url-service
  namespace: phishing-shield
spec:
  replicas: 2
  selector:
    matchLabels:
      app: url-service
  template:
    metadata:
      labels:
        app: url-service
    spec:
      containers:
      - name: url-service
        image: phishing-shield/url-service:2.0.0
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: url-service
  namespace: phishing-shield
spec:
  selector:
    app: url-service
  ports:
  - port: 8000
    targetPort: 8000
